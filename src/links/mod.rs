use anyhow::Result;

use crate::util;

pub fn init() -> Result<()> {

    //let links_path = util::envs::links_folder();

    //util::filesystem::create_folder(&links_path);

    Ok(())
}

pub fn add() -> Result<()> {

    Ok(())
}